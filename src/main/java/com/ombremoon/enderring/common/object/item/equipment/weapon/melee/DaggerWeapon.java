package com.ombremoon.enderring.common.object.item.equipment.weapon.melee;

public class DaggerWeapon extends MeleeWeapon {
    public DaggerWeapon(Properties pProperties) {
        super(-1.3F, pProperties);
    }
}
